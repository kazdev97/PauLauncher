# Copia este archivo a `client_id.env` (mismo directorio) si no usas la variable
# de entorno NEXUS_CLIENT_ID, y escribe dentro SOLO el client id de tu app Azure.
# `client_id.env` está en .gitignore: NUNCA se sube al repositorio.